/*
    Default minimal config for minimemv

    Comments are not part of json file specification
    but they are stripped by config npm package
    see https://github.com/lorenwest/node-config for details
*/
{
    "instance": {},
    "extract": {
        "type": "debug",
        "config": {
            "envelopes": []
        }
    },
    "transform": [],
    "load": [{
        "type": "debug",
        "config": {
            "tag": "debug"
        }
    }]
}